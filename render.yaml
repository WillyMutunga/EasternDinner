services:
  - type: web
    name: dinner-backend
    runtime: node
    rootDir: backend
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: dinner-db
          property: connectionString
      - key: CORS_ORIGIN
        value: https://dinner-frontend.onrender.com
      - key: JWT_SECRET
        generateValue: true

  - type: web
    name: dinner-frontend
    runtime: static
    rootDir: frontend
    buildCommand: VITE_API_URL=https://dinner-backend-1khi.onrender.com npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://dinner-backend-1khi.onrender.com

databases:
  - name: dinner-db
    plan: free
    databaseName: dinner
    user: dinner_user
